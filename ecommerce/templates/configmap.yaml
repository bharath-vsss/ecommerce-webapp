apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    app: {{ include "ecommerce.fullname" . | default .Release.Name }}
data:
  {{- range $key, $value := .Values.configData }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}